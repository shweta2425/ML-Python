# Write a Python program to get the current value of the recursion limit.

import sys
#return max depth of python interpreter stack
print("Current value of the recursion limit:",sys.getrecursionlimit())
